run_command() {
  output=$("$@" 2>&1)
  status=$?
  if [ $status -eq 0 ]; then
    echo "✅ $1 successful!"
  else
    echo "❌ $1 failed!"
    echo "$output"
    return 1
  fi
}

echo "\n📋 Checking commit messages..."
#run_command npx --no-install commitlint --edit $1